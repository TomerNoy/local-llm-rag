# Query Pipeline Dependencies
# For RAG querying with PydanticAI + OpenAI API

# Core dependencies
pydantic-ai>=0.0.14
pydantic>=2.0.0

# LanceDB and embeddings (reuse same versions as ingest)
lancedb>=0.4.0
sentence-transformers>=2.2.2
pyarrow>=14.0.0

# OpenAI client (included in pydantic-ai but explicit for clarity)
openai>=1.0.0
httpx>=0.25.0
