# Query Pipeline Dependencies
# For RAG querying with PydanticAI + Ollama

# Core dependencies
pydantic-ai>=0.0.14
pydantic>=2.0.0

# LanceDB and embeddings (reuse same versions as ingest)
lancedb>=0.4.0
sentence-transformers>=2.2.2
pyarrow>=14.0.0

# Ollama client (included in pydantic-ai but explicit for clarity)
# httpx is used by pydantic-ai for Ollama communication
httpx>=0.25.0
